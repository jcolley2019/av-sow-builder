# SOW Standards

House-style rules and worked examples that guide SOW generation.

This folder is committed to the repo (unlike `/templates`, which is local-only).
Content — style guides, section ordering, boilerplate clauses, example
input/output pairs — is added in a later phase.
